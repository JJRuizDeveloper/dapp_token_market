const initialState = {
    managerAddress: "0x4a27b4beb512f3d0ca97e7a7a7cfb390a1d46aec"
}

const addresses = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default addresses;